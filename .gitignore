# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
env/

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.tox/

# uv
.uv-cache/
uv.lock

# Editor
.vscode/
.idea/
*.swp

# OS
.DS_Store

# Project data — never commit live DB, env, or credentials
*.db
*.db-shm
*.db-wal
data/
# Ignore every .env variant (incl. backups like .env._bak) so no secrets are
# ever committed; the credential-free example is the one exception.
.env
.env.*
!.env.example

# Challenge prompt / meta files — not part of the deliverable
instruction.md
guardrail.md
